class Journey {
  String transportName;
  int timeInMin;
  int price;

  Journey(transportName, timeInMin, price) {
    this.transportName = transportName;
    this.timeInMin = timeInMin;
    this.price = price;
  }
}
